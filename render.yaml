services:
  - type: web
    name: all-finance-contract
    runtime: node
    plan: free
    buildCommand: echo "No build required"
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 24.14.1
      - key: NODE_ENV
        value: production
