# Render build qotib qolishini tuzatish

1. GitHub repository ildiziga ushbu fayllarni yuklang: `package.json`, `render.yaml`, `.node-version`.
2. Render > Environment da `NODE_VERSION=24.14.1` borligini tekshiring.
3. Render > Manual Deploy > Clear build cache & deploy.
4. Loglarda `Using Node.js version 24.14.1` va `npm ci` chiqishi kerak.
